{
  "name": "mailsuitefree",
  "version": "0.1.0",
  "private": true,
  "description": "Chrome extension that removes the Mailsuite promotional signature from outgoing Gmail messages",
  "license": "MIT",
  "scripts": {
    "test": "node --test \"test/**/*.test.js\""
  },
  "devDependencies": {
    "jsdom": "^24.1.0"
  }
}
